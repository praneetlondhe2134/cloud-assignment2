datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model EscapeRoom {
  id          Int      @id @default(autoincrement())
  title       String
  description String
  difficulty  String
  createdAt   DateTime @default(now())
}
